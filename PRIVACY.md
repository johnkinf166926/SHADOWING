# 隐私与本地数据

这是单用户 Local First 工具。默认实现不包含分析追踪、广告 SDK、远程语音识别或第三方账户系统。

## 数据存放

- 教材 PDF：仅放在 `private_content/`，应用不上传、不解析。
- 教材音频与录音：本地开发时存入项目专属 R2 模拟状态；部署时存入该 Sites 项目的私有 R2 绑定。
- 内容、练习、听写与复习记录：本地 Prisma 工具使用 `data/shadowing-coach.db`；应用运行时使用项目 D1。
- 浏览器设置：主题、少量播放器偏好和 Service Worker 缓存。

上述本地目录、`.wrangler/` 与 `.env` 都被 Git 忽略。请不要手动强制提交。

## 麦克风

只有进入录音或角色扮演并主动开始时，浏览器才请求麦克风。录音先保存在当前页面内存中；点击保存后才发送到应用自己的 R2 存储。当前版本不会把音频发送给外部评分服务。

## 离线缓存

应用外壳会被自动缓存。教材音频默认不会缓存；只有点击“离线保存此课程”后才会写入浏览器 Cache Storage。清除站点数据会移除这些副本。

## 删除与备份

- 页面中的录音可单条删除并重新录制。
- 本地 Prisma 数据库可通过复制 `data/` 备份。
- 删除整个 `.wrangler/` 会清空本地运行时的 D1/R2 模拟状态，此操作不可由应用撤销。
- 部署数据应通过托管平台的 D1/R2 备份与删除能力管理。

## 外部语音服务

`.env.example` 预留了服务端 provider、endpoint 与 key，但当前只启用 `manual` provider。未来若接入第三方，应在界面明确披露提供方、传输字段、保留期限与删除方式，并在发送前获得用户同意。密钥不得出现在客户端 bundle 或版本库。

## 版权

教材正文、PDF 与出版社音频通常受版权保护。仓库和部署只应包含你有权使用的材料。内置示例为本项目生成的虚构内容，不来自附带教材。
